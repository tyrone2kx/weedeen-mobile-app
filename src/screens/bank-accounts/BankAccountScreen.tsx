import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const BankAccountScreen = () => {
  return (
    <View>
      <Text>BankAccountScreen</Text>
    </View>
  );
};

export default BankAccountScreen;

const styles = StyleSheet.create({});
